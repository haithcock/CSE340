<!--Content heading (lines 2-6)-->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>
    

    <!--A message block (line 9)-->
    <!--<%# messages() %>-->


    <!--The inventory Items Display (line 13)-->
  <!-- views/inventory/classification.ejs -->
<main>
  <!-- Existing vehicle grid -->
  <%- include('../partials/vehicle-grid') %> 
  
  <!-- Add Test Drive Button ONLY on detail pages -->
  <% if (locals.loggedin && context.isDetailView && vehicles.length > 0) { %>
    <div class="test-drive-section">
      <a href="/appointments/schedule/<%= vehicles[0].inv_id %>" class="btn">
        Schedule Test Drive
      </a>
    </div>
  <% } %>
</main>